import { EmailRegistrationModel } from './email-registration-model';

describe('EmailRegistrationModel', () => {
  it('should create an instance', () => {
    expect(new EmailRegistrationModel()).toBeTruthy();
  });
});
