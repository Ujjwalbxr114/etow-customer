import { LocationErrorNoMap } from './location-error-no-map';

describe('LocationErrorNoMap', () => {
  it('should create an instance', () => {
    expect(new LocationErrorNoMap()).toBeTruthy();
  });
});
